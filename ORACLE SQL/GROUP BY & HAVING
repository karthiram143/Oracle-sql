-- GROUP BY CLAUSE & HAVIN CLASUE
Group by is used to group rows that have the same valuesgroup by is used with aggregate functions(min, max..)
-- HAVING CLAUSE:
Having classes is used to restrict the result returned by the group by clausehaving clause acts same as where clause
-- SYN:
SELECT COLUMN,,COLUMN
FROM TABLE-NAME 
GROUP BY COLUMN
HAVING
-- EXAMPLE:
SELECT DEPARTMENT_ID, MAX(SALARY) 
FROM EMPLOYEE 
GROUP BY DEPARTMENT_ID
-- RESULT:(MAXIMUM FOR THE SALRY BUYING THE DEPARTMENT BASIS)
DEPARTMENT_ID	MAX(SALARY)
50	59000
40	54000
30	72000
70	75000
10	55000
20	67000
60	68000

-- BASED ON THE DEPARTMENT CHECK EMPLOYEE COUNT WITH MORE THEN 3 EMPLOYEE
SELECT DEPARTMENT_ID, COUNT(EMPLOYEE_ID) 
FROM EMPLOYEE 
GROUP BY DEPARTMENT_ID
HAVING COUNT(EMPLOYEE_ID) >3

-- NOT WORKING(WHILE USING THE ALIAS  IN HAVING IS NOT WORKING )
SELECT DEPARTMENT_ID, COUNT(EMPLOYEE_ID) AS EMPLOYEE
FROM EMPLOYEE 
GROUP BY DEPARTMENT_ID
HAVING EMPLOYEE >3
-- RESULUT:
DEPARTMENT_ID	COUNT(EMPLOYEE_ID)
40	5
30	6
10	7
20	6
